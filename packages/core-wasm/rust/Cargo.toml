[package]
name = "color_kit_core_wasm"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Experimental WASM kernels for color-kit"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
wasm-bindgen = "0.2"

import { ComponentPreview } from '@/components/component-preview';
import { ApiTable } from '@/components/api-table';
import { componentApiDocs } from './component-api';
import { ColorAreaDemo } from '@/components/component-demos';

# Color Area

`ColorArea` is a two-dimensional control for channel pairs like chroma/lightness.

<ComponentPreview>
  <ColorAreaDemo />
</ComponentPreview>

## API

<ApiTable rows={componentApiDocs.colorArea} />

## Usage

```tsx
import {
  Background,
  ColorArea,
  ColorPlane,
  ContrastRegionLayer,
  ContrastRegionFill,
  FallbackPointsLayer,
  GamutBoundaryLayer,
} from 'color-kit/react';

<ColorArea axes={{ x: { channel: 'l' }, y: { channel: 'c' } }}>
  <Background checkerboard />
  <ColorPlane />
  <GamutBoundaryLayer gamut="display-p3" showPathPoints />
  <ContrastRegionLayer threshold={4.5} showPathPoints />
  <ContrastRegionLayer threshold={4.5} showPathPoints>
    <ContrastRegionFill
      fillColor="#c0e1ff"
      fillOpacity={0.14}
      dotOpacity={0.2}
    />
  </ContrastRegionLayer>
  <FallbackPointsLayer />
</ColorArea>;
```

## Non-UI API helpers

- `ColorApi.resolveColorAreaAxes`
- `ColorApi.resolveColorAreaRange`
- `ColorApi.getColorAreaThumbPosition`
- `ColorApi.colorFromColorAreaPosition`
- `ColorApi.colorFromColorAreaKey`
